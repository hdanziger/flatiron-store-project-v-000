<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title %> | $<%= item.price.to_f/100 %> | <%= button_to 'Add to Cart', item_path(item) %></p>
<% end %>
</ul>